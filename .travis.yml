language: julia
os:
  - linux
  - osx
julia:
  - release
  - nightly
notifications:
  email: false
sudo: false
cache:
  directories:
  - $HOME/.julia/*/CoinOptServices/deps
addons:
  apt_packages:
  - gfortran
env:
  - MAKEFLAGS="-j3"
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("CoinOptServices"); Pkg.test("CoinOptServices")'
